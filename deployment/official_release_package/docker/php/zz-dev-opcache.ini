; يُحمّل بعد custom.ini (تسمية zz-) — لربط الكود من المضيف دون إعادة تشغيل php-fpm.
; الإنتاج: لا تُركّب هذا الملف في compose الإنتاج؛ اترك validate_timestamps=0 في custom.ini فقط.
opcache.validate_timestamps = 1
opcache.revalidate_freq     = 2
