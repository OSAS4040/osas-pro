# Deprecated: use `.env.example` in this folder (Laravel canonical name).
#
# cp backend/.env.example backend/.env
# php artisan key:generate
#
# For Docker, set DB_HOST=postgres (and Redis host redis) in backend/.env to match docker-compose.
