# تقرير اختبار التحميل k6 — WorkshopOS

- **وقت التشغيل (UTCRough):** 2026-03-31T04-38-49-420Z
- **ملاحظة المدة:** راجع حقل iteration في ملف JSON أو مخرجات stdout لـ k6.

## مؤشرات الأداء

| المقياس | القيمة |
|--------|--------|
| معدل فشل HTTP | 100.00% |
| p90 المدة | 339 ms |
| p95 المدة | 346 ms |
| التكرارات | 0 |

## التحقق الوظيفي ضمن الحمل

- **عزل المستأجر (403/404 عبر شركة الجار):** راقب معدل `tenant_isolation_403` في summary JSON.
- **POS:** راقب `pos_sale_2xx` — يتطلب عميلاً ومنتجاً في مستأجر A (بعد seed).
- **سلامة ما بعد البيع:** اتجاه `invoice_follow_ms` للقراءة المباشرة للفاتورة.

## تقدير الجاهزية (محلي / docker)

⚠️ يحتاج مراجعة

### ملاحظات

- هذا الاختبار **واقعي على مستوى HTTP**؛ للتحقق من سلامة أرصدة DB بعد ضغط شديد يُنصح بتشغيل `php artisan test` أو تدقيق يدوي.
- **اختناقات محتملة:** PHP-FPM workers، PostgreSQL connections، Redis، شبكة Docker — راقب موارد الحاويات أثناء التشغيل.
- **أولويات تحسين:** زيادة `pm.max_children`، توليد الفهارس، طوابير للعمليات غير المتزامنة، read replicas عند الإنتاج.

## البيئة (تذكير)

- متغيرات **Sentry** غير المضبوطة تُظهر تحذيرات Docker فقط ولا توقف الاختبار.
- حقل `version` في `docker-compose.yml` مهمل في إصدارات Compose الحديثة.
- اختبارات Laravel تُشغَّل عبر Docker (`docker compose exec app php artisan test`).

---
*ملف مُولَّد آلياً بواسطة k6 handleSummary*
