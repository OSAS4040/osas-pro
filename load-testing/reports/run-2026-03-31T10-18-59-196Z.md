# تقرير اختبار التحميل — WorkshopOS

| البند | القيمة |
|--------|--------|
| **الملف الشخصي (K6_PROFILE)** | `stress` |
| **وصف خط الأساس المعتمد** | Stress — تصعيد 15→130 VU على مراحل |
| **زمن التقرير** | 2026-03-31T10-18-59-196Z |

## معايير القبول (thresholds) المعتمدة في k6

- `client_timeout_or_network` → `rate<0.20` → **نجح**
- `http_req_failed` → `rate<0.35` → **نجح**
- `server_errors_5xx` → `rate<0.25` → **نجح**
- `tenant_isolation_403` → `rate>0.70` → **نجح**
- `http_req_duration` → `p(99)<60000` → **نجح**

| ملخص | العدد |
|------|------|
| عتبات ناجحة | 5 |
| عتبات فاشلة | 0 |

## مؤشرات رئيسية (عامة)

| مؤشر | قيمة |
|------|------|
| operational_http_success | 100.000% |
| http_req_failed (k6، يشمل 4xx) | 0.000% |
| server_errors_5xx | 0.000% |
| client_timeout_or_network | 0.000% |
| p50 ms | 420 |
| p95 ms | 948 |
| p99 ms | 1515 |

### تصنيف الأخطاء (لا يُكتفى بمؤشر فشل عام واحد)

| الصنف | نسبة الطلبات (تقريباً) | ملاحظة |
|--------|------------------------|--------|
| **شبكة / مهلة / بدون استجابة** | 0.000% من الطلبات | status = 0 |
| **5xx خادم** | 0.000% من الطلبات | يطابق مؤشر server_errors_5xx تقريباً |
| **4xx عميل** | 0.000% من الطلبات | يشمل 403/404 على مسار العزل — **متوقع**؛ مؤشر `http_req_failed` في k6 يعدّها «فشلاً» بينما `operational_http_success` يعتبرها نجاحاً تشغيلياً |
| **اتساق قراءة بعد كتابة (RAW)** | انظر القسم التالي | معدل نجاح لسلسلة POS→GET فقط |

### اتساق read-after-write بعد POS / فاتورة

| المؤشر | القيمة |
|--------|--------|
| **نجاح RAW** (GET /invoices/{id} يطابق البيع بعد 201) | **—** |
| **مبيعات POS بنجاح (2xx/201)** | — |
| **زمن متابعة الفاتورة** invoice_follow_ms p95 | — |

- **قراءة تنفيذية:** إذا انخفض نجاح RAW عن عتبة الملف الشخصي، لا يُعتبر مسار البيع→الفاتورة جاهزاً للذروة دون تحقيق سبب (تأخير نشر، replica، تعارض، إلخ).

### SLO حسب الرحلة (دقة تشغيلية أعلى)

| الرحلة | p95 (فعلي/حد) | p95 ضمن SLO؟ | النجاح (فعلي/حد) | نجاح ضمن SLO؟ |
|--------|----------------|----------------|-------------------|----------------|
| قراءات تشغيلية | 948 ms / 900 ms | لا | 100.00% / 99.00% | نعم |
| POS (POST /v1/pos/sale) | — / 1200 ms | لا | — / 98.00% | لا |
| Read-after-write بعد POS | — / 1000 ms | لا | — / 98.00% | لا |
| عزل المستأجر (403/404 متوقع) | — / 900 ms | لا | 0.00% / 98.00% | لا |

**مناطق اختناق مرشحة:**
- قراءات تشغيلية: راجع scen_read_mix_http_ms وread_mix_operational_ok.
- POS (POST /v1/pos/sale): راجع scen_pos_post_http_ms وpos_sale_2xx.
- Read-after-write بعد POS: راجع scen_pos_invoice_get_http_ms وraw_read_after_write_ok.
- عزل المستأجر (403/404 متوقع): راجع scen_isolation_http_ms وtenant_isolation_403.

### حدود آمنة تشغيلية مُقتَطفة من الشرائح

*(مقارنة نسبية على **هذه** البيئة؛ الحكم النهائي يبقى على التشغيل الموثّق.)*

- **قراءات (مزيج API للوحة/عملاء/مخزون/…):** `read_mix`: معدل عدم تحقق التوقّع 0.00%، p50 420 ms، p95 948 ms
- **POS وما يرتبط بالكتابة المالية:** لا بيانات بعد — تأكد من تشغيل سيناريوهات القراءة/POS/العزل في هذا الملف الشخصي.
- **مسارات حساسة (عزل tenant):** لا بيانات بعد — تأكد من تشغيل سيناريوهات القراءة/POS/العزل في هذا الملف الشخصي.

- **إجهاد:** مزيج القراءة يُقسّم حسب المراحل في جدول stress أدناه؛ صف `read_mix` المخصّص غير معروض هنا لأنه يُجمّع على كل المراحل.

## تحليل إجهاد مرحلي (stress_s15 … stress_s130)

- **تدهور غير مقبول:** معدل فشل HTTP > 5% أو p95 > 8000 ms لمرحلة.
- **انهيار تقريبي:** فشل > 25% أو p95 > 30000 ms.

| VU | fail rate | p95 (ms) | تدهور؟ | انهيار؟ |
|----|-----------|----------|--------|--------|
| 15 | — | — | لا | لا |
| 30 | — | — | لا | لا |
| 50 | — | — | لا | لا |
| 70 | — | — | لا | لا |
| 100 | — | — | لا | لا |
| 130 | — | — | لا | لا |

- **أول تدهور:** لم يُلاحظ ضمن العتبات أعلاه
- **أول انهيار:** لم يُلاحظ

## ما نجح / ما فشل (ملخص تنفيذي)

- **نجح:** 5 من 5 عتبة معرّفة في التقرير.
- **فشل:** 0 عتبة — راجع الجدول أعلاه والـ JSON.
- **سلامة بيانات / تكرار مالي:** يتطلب تدقيق DB خارج k6؛ طبقياً: مفاتيح Idempotency فريدة للبيع، وسيناريو 409 لنفس المفتاح بجسم مختلف، وفحص عزل `/companies/{id}`.

## الحد الآمن الحالي (تقدير من هذا التشغيل)

لم يُكتشف تدهور ضمن مراحل الإجهاد الحالية — يمكن رفع السقف أو إطالة المراحل.

## البيئة

- Sentry غير المضبوط → تحذيرات compose فقط.
- `version` في docker-compose مهمل.
- Laravel tests عبر Docker.

---
*مُولّد من k6 handleSummary — الملف الشخصي: stress*
