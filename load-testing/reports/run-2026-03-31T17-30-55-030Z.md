# تقرير اختبار التحميل — WorkshopOS

| البند | القيمة |
|--------|--------|
| **الملف الشخصي (K6_PROFILE)** | `peak` |
| **وصف خط الأساس المعتمد** | Peak Load — 75 VU read + POS، 12 دقيقة (50–100 / 10–15) |
| **زمن التقرير** | 2026-03-31T17-30-55-030Z |

## معايير القبول (thresholds) المعتمدة في k6

- `raw_read_after_write_ok` → `rate>0.75` → **نجح**
- `tenant_isolation_403` → `rate>0.85` → **نجح**
- `http_req_duration` → `p(50)<1200` → **نجح**
- `http_req_duration` → `p(95)<6000` → **نجح**
- `http_req_duration` → `p(99)<15000` → **نجح**
- `operational_http_success` → `rate>0.97` → **نجح**
- `server_errors_5xx` → `rate<0.02` → **نجح**
- `pos_sale_2xx` → `rate>0.60` → **نجح**
- `health_ok` → `rate>0.70` → **نجح**
- `client_timeout_or_network` → `rate<0.04` → **نجح**

| ملخص | العدد |
|------|------|
| عتبات ناجحة | 10 |
| عتبات فاشلة | 0 |

## مؤشرات رئيسية (عامة)

| مؤشر | قيمة |
|------|------|
| operational_http_success | 100.000% |
| http_req_failed (k6، يشمل 4xx) | 0.252% |
| server_errors_5xx | 0.000% |
| client_timeout_or_network | 0.000% |
| p50 ms | 547 |
| p95 ms | 1946 |
| p99 ms | 3085 |

### تصنيف الأخطاء (لا يُكتفى بمؤشر فشل عام واحد)

| الصنف | نسبة الطلبات (تقريباً) | ملاحظة |
|--------|------------------------|--------|
| **شبكة / مهلة / بدون استجابة** | 0.000% من الطلبات | status = 0 |
| **5xx خادم** | 0.000% من الطلبات | يطابق مؤشر server_errors_5xx تقريباً |
| **4xx عميل** | 0.252% من الطلبات | يشمل 403/404 على مسار العزل — **متوقع**؛ مؤشر `http_req_failed` في k6 يعدّها «فشلاً» بينما `operational_http_success` يعتبرها نجاحاً تشغيلياً |
| **اتساق قراءة بعد كتابة (RAW)** | انظر القسم التالي | معدل نجاح لسلسلة POS→GET فقط |

### اتساق read-after-write بعد POS / فاتورة

| المؤشر | القيمة |
|--------|--------|
| **نجاح RAW** (GET /invoices/{id} يطابق البيع بعد 201) | **100.00%** |
| **مبيعات POS بنجاح (2xx/201)** | 100.00% |
| **زمن متابعة الفاتورة** invoice_follow_ms p95 | 2127 ms |

- **قراءة تنفيذية:** إذا انخفض نجاح RAW عن عتبة الملف الشخصي، لا يُعتبر مسار البيع→الفاتورة جاهزاً للذروة دون تحقيق سبب (تأخير نشر، replica، تعارض، إلخ).

### SLO حسب الرحلة (دقة تشغيلية أعلى)

| الرحلة | p95 (فعلي/حد) | p95 ضمن SLO؟ | النجاح (فعلي/حد) | نجاح ضمن SLO؟ |
|--------|----------------|----------------|-------------------|----------------|
| قراءات تشغيلية | 1687 ms / 900 ms | لا | 100.00% / 99.00% | نعم |
| POS (POST /v1/pos/sale) | 3594 ms / 1200 ms | لا | 100.00% / 98.00% | نعم |
| Read-after-write بعد POS | 2125 ms / 1000 ms | لا | 100.00% / 98.00% | نعم |
| عزل المستأجر (403/404 متوقع) | 2022 ms / 900 ms | لا | 100.00% / 98.00% | نعم |

**مناطق اختناق مرشحة:**
- قراءات تشغيلية: راجع scen_read_mix_http_ms وread_mix_operational_ok.
- POS (POST /v1/pos/sale): راجع scen_pos_post_http_ms وpos_sale_2xx.
- Read-after-write بعد POS: راجع scen_pos_invoice_get_http_ms وraw_read_after_write_ok.
- عزل المستأجر (403/404 متوقع): راجع scen_isolation_http_ms وtenant_isolation_403.

### حدود آمنة تشغيلية مُقتَطفة من الشرائح

*(مقارنة نسبية على **هذه** البيئة؛ الحكم النهائي يبقى على التشغيل الموثّق.)*

- **قراءات (مزيج API للوحة/عملاء/مخزون/…):** `read_mix`: معدل عدم تحقق التوقّع 0.00%، p50 527 ms، p95 1687 ms
- **POS وما يرتبط بالكتابة المالية:** `pos_sale POST`: معدل عدم تحقق التوقّع 0.00%، p50 729 ms، p95 3594 ms؛ `invoice GET بعد البيع (RAW)`: معدل عدم تحقق التوقّع 0.00%، p50 595 ms، p95 2125 ms
- **مسارات حساسة (عزل tenant):** `عزل cross-tenant (رفض 403/404)`: معدل عدم تحقق التوقّع 0.00%، p50 520 ms، p95 2022 ms
## ما نجح / ما فشل (ملخص تنفيذي)

- **نجح:** 10 من 10 عتبة معرّفة في التقرير.
- **فشل:** 0 عتبة — راجع الجدول أعلاه والـ JSON.
- **سلامة بيانات / تكرار مالي:** يتطلب تدقيق DB خارج k6؛ طبقياً: مفاتيح Idempotency فريدة للبيع، وسيناريو 409 لنفس المفتاح بجسم مختلف، وفحص عزل `/companies/{id}`.

## الحد الآمن الحالي (تقدير من هذا التشغيل)

Peak يمر — الحد الآمن التقريبي: **حتى ~75 VU** قراءة لهذا المزيج (راقب 5xx وقاعدة البيانات).

## البيئة

- Sentry غير المضبوط → تحذيرات compose فقط.
- `version` في docker-compose مهمل.
- Laravel tests عبر Docker.

---
*مُولّد من k6 handleSummary — الملف الشخصي: peak*
