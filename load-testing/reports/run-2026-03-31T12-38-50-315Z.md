# تقرير اختبار التحميل — WorkshopOS

| البند | القيمة |
|--------|--------|
| **الملف الشخصي (K6_PROFILE)** | `soak` |
| **وصف خط الأساس المعتمد** | Soak — 22 VU قراءة × 3 شرائح 30 دقيقة + POS/صحة/عزل موازية (إجمالي ~90 دقيقة) |
| **زمن التقرير** | 2026-03-31T12-38-50-315Z |

## معايير القبول (thresholds) المعتمدة في k6

- `client_timeout_or_network` → `rate<0.015` → **نجح**
- `pos_sale_2xx` → `rate>0.70` → **نجح**
- `tenant_isolation_403` → `rate>0.88` → **نجح**
- `health_ok` → `rate>0.75` → **نجح**
- `http_req_duration` → `p(95)<4000` → **نجح**
- `http_req_duration` → `p(99)<10000` → **نجح**
- `http_req_duration` → `p(50)<900` → **نجح**
- `raw_read_after_write_ok` → `rate>0.80` → **نجح**
- `server_errors_5xx` → `rate<0.01` → **نجح**
- `operational_http_success` → `rate>0.99` → **نجح**

| ملخص | العدد |
|------|------|
| عتبات ناجحة | 10 |
| عتبات فاشلة | 0 |

## مؤشرات رئيسية (عامة)

| مؤشر | قيمة |
|------|------|
| operational_http_success | 99.999% |
| http_req_failed (k6، يشمل 4xx) | 0.055% |
| server_errors_5xx | 0.000% |
| client_timeout_or_network | 0.001% |
| p50 ms | 174 |
| p95 ms | 422 |
| p99 ms | 696 |

### تصنيف الأخطاء (لا يُكتفى بمؤشر فشل عام واحد)

| الصنف | نسبة الطلبات (تقريباً) | ملاحظة |
|--------|------------------------|--------|
| **شبكة / مهلة / بدون استجابة** | 0.001% من الطلبات | status = 0 |
| **5xx خادم** | 0.000% من الطلبات | يطابق مؤشر server_errors_5xx تقريباً |
| **4xx عميل** | 0.054% من الطلبات | يشمل 403/404 على مسار العزل — **متوقع**؛ مؤشر `http_req_failed` في k6 يعدّها «فشلاً» بينما `operational_http_success` يعتبرها نجاحاً تشغيلياً |
| **اتساق قراءة بعد كتابة (RAW)** | انظر القسم التالي | معدل نجاح لسلسلة POS→GET فقط |

### اتساق read-after-write بعد POS / فاتورة

| المؤشر | القيمة |
|--------|--------|
| **نجاح RAW** (GET /invoices/{id} يطابق البيع بعد 201) | **100.00%** |
| **مبيعات POS بنجاح (2xx/201)** | 100.00% |
| **زمن متابعة الفاتورة** invoice_follow_ms p95 | 503 ms |

- **قراءة تنفيذية:** إذا انخفض نجاح RAW عن عتبة الملف الشخصي، لا يُعتبر مسار البيع→الفاتورة جاهزاً للذروة دون تحقيق سبب (تأخير نشر، replica، تعارض، إلخ).

### SLO حسب الرحلة (دقة تشغيلية أعلى)

| الرحلة | p95 (فعلي/حد) | p95 ضمن SLO؟ | النجاح (فعلي/حد) | نجاح ضمن SLO؟ |
|--------|----------------|----------------|-------------------|----------------|
| قراءات تشغيلية | 421 ms / 900 ms | نعم | 100.00% / 99.00% | نعم |
| POS (POST /v1/pos/sale) | 877 ms / 1200 ms | نعم | 100.00% / 98.00% | نعم |
| Read-after-write بعد POS | 502 ms / 1000 ms | نعم | 100.00% / 98.00% | نعم |
| عزل المستأجر (403/404 متوقع) | 498 ms / 900 ms | نعم | 100.00% / 98.00% | نعم |

**مناطق اختناق مرشحة:**
- لا اختناق واضح ضمن حدود SLO المعرفة لهذا التشغيل.

### حدود آمنة تشغيلية مُقتَطفة من الشرائح

*(مقارنة نسبية على **هذه** البيئة؛ الحكم النهائي يبقى على التشغيل الموثّق.)*

- **قراءات (مزيج API للوحة/عملاء/مخزون/…):** `read_mix`: معدل عدم تحقق التوقّع 0.00%، p50 173 ms، p95 421 ms
- **POS وما يرتبط بالكتابة المالية:** `pos_sale POST`: معدل عدم تحقق التوقّع 0.00%، p50 325 ms، p95 877 ms؛ `invoice GET بعد البيع (RAW)`: معدل عدم تحقق التوقّع 0.00%، p50 198 ms، p95 502 ms
- **مسارات حساسة (عزل tenant):** `عزل cross-tenant (رفض 403/404)`: معدل عدم تحقق التوقّع 0.00%، p50 180 ms، p95 498 ms
## نقع (soak): تدهور تدريجي عبر الزمن؟

يُقارن **زمن طلبات مزيج القراءة** في ثلاث شرائح متتالية (~30 دقيقة لكل منها) عبر المؤشرات `soak_read_seg*_http_ms`.

| الشريحة | p50 (ms) | p95 (ms) |
|---------|----------|----------|
| `soak_read_seg1` | 234 | 533 |
| `soak_read_seg2` | 158 | 409 |
| `soak_read_seg3` | 149 | 268 |

- **الخلاصة:** **لا — لا دلالة قوية على تدهور تدريجي** في p95 قراءة النقع (مقارنة أول/وسط/آخر ~30 دقيقة).
- **مؤشر مساعد:** ارتفاع p95 بين أول وآخر شريحة >35% → غير مُلاحظ.


## ما نجح / ما فشل (ملخص تنفيذي)

- **نجح:** 10 من 10 عتبة معرّفة في التقرير.
- **فشل:** 0 عتبة — راجع الجدول أعلاه والـ JSON.
- **سلامة بيانات / تكرار مالي:** يتطلب تدقيق DB خارج k6؛ طبقياً: مفاتيح Idempotency فريدة للبيع، وسيناريو 409 لنفس المفتاح بجسم مختلف، وفحص عزل `/companies/{id}`.

## الحد الآمن الحالي (تقدير من هذا التشغيل)

Soak يمر — استقرار زمني جيد عند ~22 VU على مدى التشغيل.

## البيئة

- Sentry غير المضبوط → تحذيرات compose فقط.
- `version` في docker-compose مهمل.
- Laravel tests عبر Docker.

---
*مُولّد من k6 handleSummary — الملف الشخصي: soak*
