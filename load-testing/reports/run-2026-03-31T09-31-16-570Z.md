# تقرير اختبار التحميل — WorkshopOS

| البند | القيمة |
|--------|--------|
| **الملف الشخصي (K6_PROFILE)** | `smoke` |
| **وصف خط الأساس المعتمد** | Smoke — 8 VU, 4 دقائق (ضمن 5–10 / 3–5) |
| **زمن التقرير** | 2026-03-31T09-31-16-570Z |

## معايير القبول (thresholds) المعتمدة في k6

- `http_req_duration` → `p(50)<600` → **نجح**
- `http_req_duration` → `p(95)<2200` → **نجح**
- `http_req_duration` → `p(99)<6000` → **نجح**
- `client_timeout_or_network` → `rate<0.01` → **نجح**
- `operational_http_success` → `rate>0.99` → **نجح**
- `tenant_isolation_403` → `rate>0.90` → **نجح**
- `idempotency_409_payload_mismatch` → `rate>0.80` → **نجح**
- `server_errors_5xx` → `rate<0.005` → **نجح**
- `raw_read_after_write_ok` → `rate>0.80` → **نجح**
- `health_ok` → `rate>0.85` → **نجح**

| ملخص | العدد |
|------|------|
| عتبات ناجحة | 10 |
| عتبات فاشلة | 0 |

## مؤشرات رئيسية (عامة)

| مؤشر | قيمة |
|------|------|
| operational_http_success | 100.000% |
| http_req_failed (k6، يشمل 4xx) | 4.140% |
| server_errors_5xx | 0.000% |
| client_timeout_or_network | 0.000% |
| p50 ms | 155 |
| p95 ms | 263 |
| p99 ms | 352 |

### تصنيف الأخطاء (لا يُكتفى بمؤشر فشل عام واحد)

| الصنف | نسبة الطلبات (تقريباً) | ملاحظة |
|--------|------------------------|--------|
| **شبكة / مهلة / بدون استجابة** | 0.000% من الطلبات | status = 0 |
| **5xx خادم** | 0.000% من الطلبات | يطابق مؤشر server_errors_5xx تقريباً |
| **4xx عميل** | 4.145% من الطلبات | يشمل 403/404 على مسار العزل — **متوقع**؛ مؤشر `http_req_failed` في k6 يعدّها «فشلاً» بينما `operational_http_success` يعتبرها نجاحاً تشغيلياً |
| **اتساق قراءة بعد كتابة (RAW)** | انظر القسم التالي | معدل نجاح لسلسلة POS→GET فقط |

### اتساق read-after-write بعد POS / فاتورة

| المؤشر | القيمة |
|--------|--------|
| **نجاح RAW** (GET /invoices/{id} يطابق البيع بعد 201) | **100.00%** |
| **مبيعات POS بنجاح (2xx/201)** | 100.00% |
| **زمن متابعة الفاتورة** invoice_follow_ms p95 | 224 ms |

- **قراءة تنفيذية:** إذا انخفض نجاح RAW عن عتبة الملف الشخصي، لا يُعتبر مسار البيع→الفاتورة جاهزاً للذروة دون تحقيق سبب (تأخير نشر، replica، تعارض، إلخ).

### SLO حسب الرحلة (دقة تشغيلية أعلى)

| الرحلة | p95 (فعلي/حد) | p95 ضمن SLO؟ | النجاح (فعلي/حد) | نجاح ضمن SLO؟ |
|--------|----------------|----------------|-------------------|----------------|
| قراءات تشغيلية | 258 ms / 900 ms | نعم | 100.00% / 99.00% | نعم |
| POS (POST /v1/pos/sale) | 354 ms / 1200 ms | نعم | 100.00% / 98.00% | نعم |
| Read-after-write بعد POS | 223 ms / 1000 ms | نعم | 100.00% / 98.00% | نعم |
| عزل المستأجر (403/404 متوقع) | 237 ms / 900 ms | نعم | 100.00% / 98.00% | نعم |

**مناطق اختناق مرشحة:**
- لا اختناق واضح ضمن حدود SLO المعرفة لهذا التشغيل.

### حدود آمنة تشغيلية مُقتَطفة من الشرائح

*(مقارنة نسبية على **هذه** البيئة؛ الحكم النهائي يبقى على التشغيل الموثّق.)*

- **مزيج smoke (قراءات + POS + عزل — مُدمج في `smoke_mixed`):** زمن HTTP إجمالي p50 155 ms، p95 263 ms؛ `operational_http_success` 100.00%؛ POS 201 100.00%؛ RAW بعد البيع 100.00%؛ `http_req_failed` k6 4.14% (متوقع ارتفاعه بسبب 403 العزل).

- **تفصيل نوع الحمل (من عيّنات فعلية داخل `smoke_mixed`):**

- **قراءات (مزيج API للوحة/عملاء/مخزون/…):** `read_mix`: معدل عدم تحقق التوقّع 0.00%، p50 157 ms، p95 258 ms
- **POS وما يرتبط بالكتابة المالية:** `pos_sale POST`: معدل عدم تحقق التوقّع 0.00%، p50 211 ms، p95 354 ms؛ `invoice GET بعد البيع (RAW)`: معدل عدم تحقق التوقّع 0.00%، p50 143 ms، p95 223 ms
- **مسارات حساسة (عزل tenant):** `عزل cross-tenant (رفض 403/404)`: معدل عدم تحقق التوقّع 0.00%، p50 142 ms، p95 237 ms
- **مسارات `smoke_idempotency`:** تُراجع في العتبات و`idempotency_409_payload_mismatch`.

## ما نجح / ما فشل (ملخص تنفيذي)

- **نجح:** 10 من 10 عتبة معرّفة في التقرير.
- **فشل:** 0 عتبة — راجع الجدول أعلاه والـ JSON.
- **سلامة بيانات / تكرار مالي:** يتطلب تدقيق DB خارج k6؛ طبقياً: مفاتيح Idempotency فريدة للبيع، وسيناريو 409 لنفس المفتاح بجسم مختلف، وفحص عزل `/companies/{id}`.

## الحد الآمن الحالي (تقدير من هذا التشغيل)

Smoke يمر — جرّب **Normal** ثم **Peak** على staging.

## البيئة

- Sentry غير المضبوط → تحذيرات compose فقط.
- `version` في docker-compose مهمل.
- Laravel tests عبر Docker.

---
*مُولّد من k6 handleSummary — الملف الشخصي: smoke*
