# تقرير اختبار التحميل — WorkshopOS

| البند | القيمة |
|--------|--------|
| **الملف الشخصي (K6_PROFILE)** | `capacity_pos` |
| **وصف خط الأساس المعتمد** | Capacity POS — بيع POS فقط (معدّل ثابت عبر K6_CAPACITY_POS_RATE)، عتبات استكشافية — قارِن التقارير بين المعدلات |
| **زمن التقرير** | 2026-04-26T13-51-18-450Z |

## معايير القبول (thresholds) المعتمدة في k6

- `server_errors_5xx` → `rate<0.99` → **نجح**
- `http_req_failed` → `rate<0.995` → **نجح**
- `http_req_duration` → `p(99)<120000` → **نجح**
- `client_timeout_or_network` → `rate<0.99` → **نجح**

| ملخص | العدد |
|------|------|
| عتبات ناجحة | 4 |
| عتبات فاشلة | 0 |

## مؤشرات رئيسية (عامة)

| مؤشر | قيمة |
|------|------|
| operational_http_success | 100.000% |
| http_req_failed (k6، يشمل 4xx) | 0.000% |
| server_errors_5xx | 0.000% |
| client_timeout_or_network | 0.000% |
| p50 ms | 351 |
| p95 ms | 654 |
| p99 ms | 854 |

### تصنيف الأخطاء (لا يُكتفى بمؤشر فشل عام واحد)

| الصنف | نسبة الطلبات (تقريباً) | ملاحظة |
|--------|------------------------|--------|
| **شبكة / مهلة / بدون استجابة** | 0.000% من الطلبات | status = 0 |
| **5xx خادم** | 0.000% من الطلبات | يطابق مؤشر server_errors_5xx تقريباً |
| **4xx عميل** | 0.000% من الطلبات | يشمل 403/404 على مسار العزل — **متوقع**؛ مؤشر `http_req_failed` في k6 يعدّها «فشلاً» بينما `operational_http_success` يعتبرها نجاحاً تشغيلياً |
| **اتساق قراءة بعد كتابة (RAW)** | انظر القسم التالي | معدل نجاح لسلسلة POS→GET فقط |

### اتساق read-after-write بعد POS / فاتورة

| المؤشر | القيمة |
|--------|--------|
| **نجاح RAW** (GET /invoices/{id} يطابق البيع بعد 201) | **100.00%** |
| **مبيعات POS بنجاح (2xx/201)** | 100.00% |
| **زمن متابعة الفاتورة** invoice_follow_ms p95 | 464 ms |

- **قراءة تنفيذية:** إذا انخفض نجاح RAW عن عتبة الملف الشخصي، لا يُعتبر مسار البيع→الفاتورة جاهزاً للذروة دون تحقيق سبب (تأخير نشر، replica، تعارض، إلخ).

### SLO حسب الرحلة (دقة تشغيلية أعلى)

| الرحلة | p95 (فعلي/حد) | p95 ضمن SLO؟ | النجاح (فعلي/حد) | نجاح ضمن SLO؟ |
|--------|----------------|----------------|-------------------|----------------|
| قراءات تشغيلية | — / 900 ms | لا | — / 99.00% | لا |
| POS (POST /v1/pos/sale) | 728 ms / 1200 ms | نعم | 100.00% / 98.00% | نعم |
| Read-after-write بعد POS | 463 ms / 1000 ms | نعم | 100.00% / 98.00% | نعم |
| عزل المستأجر (403/404 متوقع) | — / 900 ms | لا | — / 98.00% | لا |

**مناطق اختناق مرشحة:**
- قراءات تشغيلية: راجع scen_read_mix_http_ms وread_mix_operational_ok.
- عزل المستأجر (403/404 متوقع): راجع scen_isolation_http_ms وtenant_isolation_403.

### حدود آمنة تشغيلية مُقتَطفة من الشرائح

*(مقارنة نسبية على **هذه** البيئة؛ الحكم النهائي يبقى على التشغيل الموثّق.)*

- **قراءات (مزيج API للوحة/عملاء/مخزون/…):** لا بيانات بعد — تأكد من تشغيل سيناريوهات القراءة/POS/العزل في هذا الملف الشخصي.
- **POS وما يرتبط بالكتابة المالية:** `pos_sale POST`: معدل عدم تحقق التوقّع 0.00%، p50 420 ms، p95 728 ms؛ `invoice GET بعد البيع (RAW)`: معدل عدم تحقق التوقّع 0.00%، p50 254 ms، p95 463 ms
- **مسارات حساسة (عزل tenant):** لا بيانات بعد — تأكد من تشغيل سيناريوهات القراءة/POS/العزل في هذا الملف الشخصي.
## ما نجح / ما فشل (ملخص تنفيذي)

- **نجح:** 4 من 4 عتبة معرّفة في التقرير.
- **فشل:** 0 عتبة — راجع الجدول أعلاه والـ JSON.
- **سلامة بيانات / تكرار مالي:** يتطلب تدقيق DB خارج k6؛ طبقياً: مفاتيح Idempotency فريدة للبيع، وسيناريو 409 لنفس المفتاح بجسم مختلف، وفحص عزل `/companies/{id}`.

## الحد الآمن الحالي (تقدير من هذا التشغيل)

تشغيل استكشافي لسعة POS — راقب `scen_pos_post_http_ms` p99 و`pos_sale_2xx` و`dropped_iterations`. قارِن عدة تشغيلات بمعدلات وصول مختلفة (مثلاً 3 / 5 / 7/sec عبر `K6_CAPACITY_POS_RATE`).

## البيئة

- Sentry غير المضبوط → تحذيرات compose فقط.
- `version` في docker-compose مهمل.
- Laravel tests عبر Docker.

---
*مُولّد من k6 handleSummary — الملف الشخصي: capacity_pos*
