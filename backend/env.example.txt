# env.example.txt
# Copy this file to .env and fill in the values.

APP_NAME="AutoService SaaS"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
APP_TIMEZONE=UTC

LOG_CHANNEL=stack
LOG_LEVEL=debug

# DATABASE
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=saas_db
DB_USERNAME=saas_user
DB_PASSWORD=saas_password

# REDIS
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_password
REDIS_DB=0
REDIS_CACHE_DB=1

# CACHE / SESSION / QUEUE
CACHE_STORE=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120
QUEUE_CONNECTION=redis

# SANCTUM
SANCTUM_STATEFUL_DOMAINS=localhost,localhost:5173,127.0.0.1

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost

# SENTRY
SENTRY_LARAVEL_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_ENVIRONMENT=local

# SWAGGER / OPENAPI
L5_SWAGGER_GENERATE_ALWAYS=false
L5_SWAGGER_CONST_HOST=http://localhost/api/v1

# FILESYSTEM / S3 (AWS)
FILESYSTEM_DISK=local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=me-south-1
AWS_BUCKET=
AWS_URL=

# MAIL
MAIL_MAILER=log
MAIL_FROM_ADDRESS=noreply@autoservice.local
MAIL_FROM_NAME="AutoService SaaS"

# IDEMPOTENCY TTL (hours)
IDEMPOTENCY_TTL_HOURS=24

# SUBSCRIPTION GRACE PERIOD (days)
SUBSCRIPTION_GRACE_PERIOD_DAYS=15
